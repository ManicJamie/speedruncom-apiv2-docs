## Parameters
- challengeId

# Response
```
challengeRunList[]
    #Run# + challengeId
playerList[]
    #Player#
userList[]
    #User#
pagination
    count
    page
    pages
    per # NB: this is 1000000 by default, lmao
```